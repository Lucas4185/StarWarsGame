using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour {
/*
	public Transform ship;
	Transform point;

	Vector2 a,b;

    void Update() {
		if(point == null) {
			point = GameObject.Find("Hover Point").transform;
		}
		a = new Vector2(point.position.x - transform.position.x, point.position.z - transform.position.z).normalized;
		b = new Vector2(ship.position.x - transform.position.x, ship.position.z - transform.position.z).normalized;

        float inproduct = a.x * b.x + a.y * b.y;

        float theta = Mathf.Acos(inproduct);
		print(theta * Mathf.Rad2Deg);
        // SetRoll(theta);
	}

	void OnDrawGizmos() {
		Gizmos.DrawSphere(new Vector3(a.x, 0f, a.y), 0.1f);
		Gizmos.DrawSphere(new Vector3(b.x, 0f, b.y), 0.1f);
	}*/

}
